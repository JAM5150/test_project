<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="menu">
	<select id="list" resultType="HashMap">
		SELECT *
		FROM MENU_LIST
	</select>
	
	<select id="one" resultType="HashMap" parameterType="HashMap">
		SELECT *
		FROM MENU_LIST
		WHERE MENU_ID=#{id}
	</select>
	
	<insert id="insert" parameterType="HashMap">
		INSERT INTO MENU_LIST
		(MENU_ID, MENU_NAME, MENU_DESC, MENU_SORT,REG_DATETIME, MOD_DATETIME)
		VALUES (#{MENU_ID}, #{MENU_NAME},#{MENU_DESC}, 9999,CURRENT_DATE ,CURRENT_DATE)
	</insert>
	
	<update id="update" parameterType="HashMap">
		UPDATE MENU_LIST
		SET
			MENU_NAME=#{MENU_NAME}
			, MENU_DESC=#{MENU_DESC}
			, MODE_DATETIME= CURRENT_DATE
		WHERE MENU_ID=#{MENU_ID}
	</update>
	
	<delete id="delete" parameterType="HashMap">
		DELETE FROM MENU_LIST WHERE MENU_ID=#{id}
	</delete>
</mapper>